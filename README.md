# WhatsApp OTP Service

This repository is Docker-first.

## Run

```bash
docker compose up --build
```

All runtime environment variables are declared in `docker-compose.yml`.

## Layout

- `app/` → all application code and docs.
- `Dockerfile`, `docker-compose.yml` → container/runtime setup.
